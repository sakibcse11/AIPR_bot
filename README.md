# AIPR_bot – Apple ID Password Reset Bot

This project is an automation bot built with **Python** and **Selenium WebDriver** to reset Apple ID passwords in bulk. It reads account credentials from an Excel file and automates the password reset process via the Apple ID recovery flow.

## 🔧 Features

- Reads Apple ID and password data from an Excel (.xlsx) file.
- Automates login and password reset steps using Selenium.
- Handles common UI elements, delays, and security prompts.
- Logs success/failure results for each attempt.

## 🚀 Technologies Used

- Python 3
- Selenium WebDriver
- openpyxl (for Excel file handling)
- ChromeDriver (or any supported WebDriver)

